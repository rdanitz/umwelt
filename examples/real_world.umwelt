# this is a simple environment spec
# with some real world values

! home : String
! path : String # this should be array maybe?
! pwd : String # this should be 'Path' maybe?

? xdg-config-home # Path?
? term : screen | xterm | st-256color ~> st-256color
? histsize : Nat ~> 1000
